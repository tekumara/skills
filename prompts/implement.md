---
description: Implementation walkthrough
argument-hint: "[capability]"
---

How would you add, design, and implement the feature capability for $ARGUMENTS?

Walk me through the code paths, data structures you would create, update, or change; the types you would add, change, or delete; and anything else relevant. I really want you to focus on code paths, types, abstractions, and data flow.

Outline what the call stack is today and how you propose changing it. At each level, state the input type, output type, side effects, and errors that can occur.

Give the narrowest end-to-end slice that proves the feature works, and show the verification a human can use to confirm it.
